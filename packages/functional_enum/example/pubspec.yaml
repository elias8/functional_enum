name: example
description: A sample command-line application.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  functional_enum_annotation:
    path: ../../functional_enum_annotation

dev_dependencies:
  build_runner:
  test: ^1.15.3
  functional_enum:
    path: ../
